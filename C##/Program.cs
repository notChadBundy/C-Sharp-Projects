using System;

namespace C__
{// the problem lies after the second string variable is used.
    class Program
    {
        static void Main(string[] args)
        {
            string misc;
            string cisco;
            Console.WriteLine("How to concatenante");
           misc = Console.ReadLine();
           Console.WriteLine("Enter another variable");
          // cisco = Console.WriteLine();
           // Console.WriteLine(misc + cisco);
        }
    }
}
